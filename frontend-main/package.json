{
  "name": "ashwini-frontend-main",
  "version": "1.0.0",
  "private": true,
  "description": "Project Ashwini - Main Frontend (Registration + Health Monitoring Station)",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "4.0.3",
    "axios": "^1.6.0",
    "bootstrap": "^5.3.0",
    "react-markdown": "^8.0.7",
    "remark-gfm": "^3.0.1"
  },
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && set SKIP_PREFLIGHT_CHECK=true && set GENERATE_SOURCEMAP=false && set PORT=4000 && react-scripts start",
    "build": "react-scripts build",
    "vercel-build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
